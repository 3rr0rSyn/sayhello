# 宝塔部署指南

本文介绍如何使用宝塔面板部署 SayHello Flask 项目。

## 环境要求

- 宝塔面板已安装
- 安装 Python 项目管理器插件
- Python 版本：3.11 / 3.12 / 3.13（推荐 3.12 或 3.13）

## 部署步骤

### 1. 上传代码

将项目代码上传到服务器目录，例如：

```
/www/wwwroot/sayhello
```

### 2. 安装 Python 版本

在宝塔面板中打开 **Python 项目管理器**，安装所需的 Python 版本（如 3.12）。

### 3. 创建 Python 项目

1. 点击 **添加项目**
2. 项目路径选择 `/www/wwwroot/sayhello`
3. Python 版本选择 3.12
4. 框架选择 **Flask**
5. 启动方式选择 **gunicorn** 或自定义命令

### 4. 配置启动命令

由于本项目没有 `app.py` / `main.py`，请使用自定义启动命令：

```bash
gunicorn -w 4 -b 127.0.0.1:5000 "sayhello:app"
```

如果使用 Python 项目管理器的 gunicorn 启动方式，WSGI 入口填写：

```
sayhello:app
```

### 5. 初始化数据库

进入项目根目录，执行：

```bash
cd /www/wwwroot/sayhello
source .venv/bin/activate
flask initdb
```

### 6. 配置反向代理

在站点设置中添加反向代理：

- 目标 URL：`http://127.0.0.1:5000`
- 发送域名：默认

### 7. 环境变量（建议配置）

在 Python 项目管理器中添加以下环境变量：

| 变量名 | 说明 | 示例 |
|--------|------|------|
| `SECRET_KEY` | Flask 密钥，用于 session 安全 | 随机长字符串 |
| `ADMIN_PASSWORD` | 后台登录密码 | 强密码 |
| `DATABASE_URI` | 数据库地址，可选 | `sqlite:////www/wwwroot/sayhello/data.db` |

> 注意：默认后台密码为 `admin`，生产环境务必修改。

### 8. 重启项目

完成以上配置后，在 Python 项目管理器中重启项目即可访问。

## 常见问题

**Q: 提示数据库表不存在？**

A: 请确保已执行 `flask initdb` 初始化数据库。

**Q: 静态文件无法加载？**

A: 如果使用 Nginx 反向代理，请确保静态文件路径正确，或在宝塔中配置静态文件规则。

**Q: 后台无法登录？**

A: 检查是否设置了 `ADMIN_PASSWORD` 环境变量，默认密码为 `admin`。
