# 宝塔部署指南

本文介绍如何使用宝塔面板部署 SayHello Flask 项目。

## 方案一：宝塔 Python 项目管理器（推荐）

截图中显示的是新版宝塔 **Python 项目管理器** 的“添加 Python 项目”弹窗。该项目没有 `app.py` / `main.py`，所以先在项目根目录补一个入口文件 `app.py`：

```python
from sayhello import app
```

### 1. 上传代码

将项目上传到服务器目录，例如：

```
/www/wwwroot/sayhello
```

### 2. 添加 Python 环境

在宝塔面板打开 **Python 项目管理器** → 环境管理 → 安装所需 Python 版本（推荐 3.12 或 3.13）。

### 3. 创建项目

点击 **添加 Python 项目**，按截图界面填写：

| 字段 | 填写内容 |
|------|----------|
| 项目名称 | `sayhello`（随意） |
| 项目端口 | `2026`（或你喜欢的端口） |
| Python 环境 | 选择你安装的 Python 版本 |
| 启动方式 | `gunicorn` |
| 项目路径 | `/www/wwwroot/sayhello` |
| 入口文件 | `/www/wwwroot/sayhello/app.py` |
| 通讯协议 | `wsgi` |
| 应用名称 | `app` |
| 启动用户 | `www` |
| 安装依赖包 | `/www/wwwroot/sayhello/requirements.txt` |

> 宝塔实际执行的命令类似：
> ```bash
> gunicorn -w 4 -b 127.0.0.1:2026 app:app
> ```
> 入口文件 `app.py` 中的 `app` 对象会被作为 WSGI 应用启动。

### 4. 环境变量（建议配置）

宝塔环境变量处选择 **指定变量** 或 **从文件加载**，添加以下变量：

| 变量名 | 说明 | 示例 |
|--------|------|------|
| `SECRET_KEY` | Flask 密钥，用于 session 安全 | 随机长字符串 |
| `ADMIN_PASSWORD` | 后台登录密码 | 强密码 |
| `DATABASE_URI` | 数据库地址，可选 | `sqlite:////www/wwwroot/sayhello/data.db` |

> 注意：默认后台密码为 `admin`，生产环境务必修改。

### 5. 初始化数据库

本项目首次访问会自动创建数据库表，因此通常不需要手动初始化。  
如需手动初始化，可在项目目录执行：

```bash
cd /www/wwwroot/sayhello
source .venv/bin/activate  # 如宝塔未自动创建虚拟环境，可先创建
flask initdb
```

> 宝塔一般会自动创建虚拟环境并安装依赖。如果它没有自动创建，你可以在项目路径下手动创建并指定虚拟环境路径。

### 6. 配置反向代理

在宝塔站点设置中添加反向代理：

- 目标 URL：`http://127.0.0.1:2026`
- 发送域名：默认

### 7. 重启项目

在 Python 项目管理器中点击重启，即可访问。

---

## 方案二：使用 Docker 部署（可选，便于分发）

如果你想把项目打包成 Docker 镜像，发给别人直接运行，可以按以下步骤操作。

### 1. 编写 Dockerfile

在项目根目录创建 `Dockerfile`：

```dockerfile
FROM python:3.12-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 5000

CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:5000", "app:app"]
```

### 2. 构建镜像

```bash
docker build -t sayhello:latest .
```

### 3. 导出镜像

```bash
docker save -o sayhello.tar sayhello:latest
```

将 `sayhello.tar` 发送给目标服务器。

### 4. 目标服务器导入并运行

在目标服务器上：

```bash
docker load -i sayhello.tar
docker run -d --name sayhello -p 5000:5000 -e ADMIN_PASSWORD=你的密码 -e SECRET_KEY=你的密钥 sayhello:latest
```

### 5. 能在宝塔里管理这个 Docker 吗？

**可以，但有条件：**

- 目标服务器必须安装宝塔面板并安装 **Docker 管理器** 插件。
- 导入镜像后，可以在宝塔 Docker 管理器中看到镜像，点击 **创建容器**。
- 配置端口映射（例如 `5000:5000`）和环境变量即可。
- 然后同样在宝塔站点中配置反向代理到 `http://127.0.0.1:5000`。

> **注意**：如果对方直接用 `docker run` 运行，而不是通过宝塔 Docker 管理器创建，那么宝塔只能看到容器，但无法像自己创建的容器一样完全图形化管理。因此建议让对方在宝塔 Docker 管理器里导入镜像并创建容器。

---

## 常见问题

**Q: 提示数据库表不存在？**

A: 正常情况下首次访问会自动建表。若仍报错，可在项目目录执行 `flask initdb`。

**Q: 静态文件无法加载？**

A: 如果使用 Nginx 反向代理，请确保静态文件路径正确，或在宝塔中配置静态文件规则。

**Q: 后台无法登录？**

A: 检查是否设置了 `ADMIN_PASSWORD` 环境变量，默认密码为 `admin`。

**Q: 入口文件填 `sayhello/__init__.py` 行不行？**

A: 不建议。宝塔会把它当普通文件执行，而 `__init__.py` 直接运行不会创建 `app` 变量。使用项目根目录的 `app.py` 最稳妥。
